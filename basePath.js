// Single source of truth for the GitHub Pages sub-path this app is served
// under. Used by next.config.js (basePath/assetPrefix) and app/layout.tsx
// (manually-specified icon/manifest paths, which Next does NOT basePath-
// prefix automatically the way it does for its own _next/ bundle assets).
module.exports = '/Contact-book';
